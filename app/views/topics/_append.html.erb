<div class="modal" id="append-modal">
  <div class="modal-dialog">
    <div class="modal-content">

      <div class="modal-header">
        <h4 class="modal-title">添加附言</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <%= form_for('', url: action_topic_path(topic, type: "append"), method: "post", remote: true) do |f| %>
      <div class="modal-body">

        <div class="alert alert-warning" role="alert">附加内容，使用此功能的话，会给所有参加过讨论和关注的人发送提醒。</div>

        <div class="form-group">
          <textarea id="append-content" class="form-control" name="append_body" rows="4" placeholder="请使用 Markdown 格式书写 ;)"></textarea>
        </div>
      </div>
      <div class="modal-footer">
        <button type="submit" class="btn btn-primary">确定</button>
        <a class="btn btn-default" data-dismiss="modal">取消</a>
      </div>
      <% end %>
    </div>
  </div>
</div>
